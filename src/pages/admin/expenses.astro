---
import AdminLayout from '../../layouts/AdminLayout.astro';
import { ExpensesManager } from '../../components/admin/ExpensesManager';
import { 
  getExpenses, 
  getAccounts, 
  getMainAccountBalance,
  getRecurringExpensesConfig 
} from '../../services/finance.service';

const expenses = await getExpenses();
const accounts = await getAccounts();
const mainBalance = await getMainAccountBalance();
const recurringConfig = await getRecurringExpensesConfig();
---

<AdminLayout title="Pengeluaran & Kas - WargaHub" currentPath="/admin/expenses">
  <ExpensesManager 
    initialExpenses={expenses} 
    initialAccounts={accounts}
    initialBalance={mainBalance}
    initialRecurringConfig={recurringConfig}
    client:load 
  />
</AdminLayout>
